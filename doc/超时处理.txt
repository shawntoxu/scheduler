测试超时
 
  每分钟执行一次
  设置1分钟超时
  失败重试1次
  允许跳过 

  发现
  跳过及执行成功 不断重复出现
  表明超过1分钟的跳过了，到下次执行就成功了，而下次并没有运行(显示尚未运行)

  如果不选择允许跳过，选择并行，那么每次都能执行成功
  如果不选择允许跳过，选择并行，失败重试设置为0  每次运行也能成功，并且会出现2个任务同时运行






